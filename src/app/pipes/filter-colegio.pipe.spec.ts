import { FilterColegioPipe } from './filter-colegio.pipe';

describe('FilterColegioPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterColegioPipe();
    expect(pipe).toBeTruthy();
  });
});
