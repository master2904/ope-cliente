import { PipeScorePipe } from './pipe-score.pipe';

describe('PipeScorePipe', () => {
  it('create an instance', () => {
    const pipe = new PipeScorePipe();
    expect(pipe).toBeTruthy();
  });
});
