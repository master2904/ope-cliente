import { FormularioProblema } from './formulario-problema';

describe('FormularioProblema', () => {
  it('should create an instance', () => {
    expect(new FormularioProblema()).toBeTruthy();
  });
});
